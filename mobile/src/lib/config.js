// Altere o IP abaixo para o IP local do seu computador para poder conectar o celular ao seu servidor local!
// No Windows, abra o prompt de comando (cmd) e digite 'ipconfig' para descobrir o seu IPv4 local.
// Exemplo comum: '192.168.1.X' ou '192.168.15.X'
export const API_IP = '192.168.0.102'; // <- Substitua pelo seu IP local!

export const API_BASE_URL = `http://${API_IP}:5000`;
